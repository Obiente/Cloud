name: Build and Push Audit Service

on:
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - "apps/audit-service/**"
      - "apps/shared/pkg/auth/**"
      - "apps/shared/pkg/database/**"
      - "apps/shared/pkg/health/**"
      - "apps/shared/pkg/logger/**"
      - "apps/shared/pkg/middleware/**"
      - "apps/shared/pkg/services/common/**"
      - "apps/shared/pkg/services/organizations/**"
      - "apps/shared/proto/obiente/cloud/audit/**"
      - "packages/proto/proto/obiente/cloud/audit/**"
      - "apps/shared/go.mod"
      - "apps/shared/go.sum"
      - ".github/workflows/build-audit-service.yml"
      - ".github/workflows/reusable-container-build.yml"
  pull_request:
    branches:
      - main
    paths:
      - "apps/audit-service/**"
      - "apps/shared/pkg/auth/**"
      - "apps/shared/pkg/database/**"
      - "apps/shared/pkg/health/**"
      - "apps/shared/pkg/logger/**"
      - "apps/shared/pkg/middleware/**"
      - "apps/shared/pkg/services/common/**"
      - "apps/shared/pkg/services/organizations/**"
      - "apps/shared/proto/obiente/cloud/audit/**"
      - "packages/proto/proto/obiente/cloud/audit/**"
      - "apps/shared/go.mod"
      - "apps/shared/go.sum"
      - ".github/workflows/build-audit-service.yml"
      - ".github/workflows/reusable-container-build.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Audit Service
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-container-build.yml
    with:
      service-name: audit-service
      display-name: Audit Service
      publish: false

  publish:
    name: Publish Audit Service
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/reusable-container-build.yml
    with:
      service-name: audit-service
      display-name: Audit Service
      publish: true
    secrets: inherit
