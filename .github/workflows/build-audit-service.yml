name: Build and Push Audit Service

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
    paths:
      - 'apps/audit-service/**'
      - 'apps/shared/**'
  pull_request:
    branches:
      - main
    paths:
      - 'apps/audit-service/**'
      - 'apps/shared/**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-audit-service:
    name: Build and Push Audit Service (${{ matrix.platform }})
    runs-on: self-hosted
    strategy:
      matrix:
        platform: [linux/amd64, linux/arm64]
      fail-fast: false
    env:
      REGISTRY: ghcr.io
      SERVICE_NAME: audit-service
      IMAGE_NAME: ghcr.io/obiente/cloud-audit-service
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        id: buildx
        with:
          driver-opts: |
            image=moby/buildkit:latest
            network=host

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha,prefix=sha-,enable=${{ github.event_name != 'pull_request' }}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Get commit message
        id: commit-msg
        run: |
          COMMIT_MSG=$(git log -1 --format=%s ${{ github.sha }})
          echo "message<<EOF" >> $GITHUB_OUTPUT
          echo "$COMMIT_MSG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      # Build and push each platform independently for non-blocking pushes
      # Each architecture runs in parallel and pushes as soon as it's ready
      - name: Build and push Audit Service Docker image (${{ matrix.platform }}) - attempt 1
        id: build-attempt-1
        uses: docker/build-push-action@v5
        continue-on-error: true
        with:
          context: .
          file: ./apps/${{ env.SERVICE_NAME }}/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            COMMIT=${{ github.sha }}
            COMMIT_MESSAGE=${{ steps.commit-msg.outputs.message }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: ${{ matrix.platform }}
          builder: ${{ steps.buildx.outputs.name }}

      - name: Build and push Audit Service Docker image (${{ matrix.platform }}) - attempt 2
        if: steps.build-attempt-1.outcome == 'failure'
        id: build-attempt-2
        uses: docker/build-push-action@v5
        continue-on-error: true
        with:
          context: .
          file: ./apps/${{ env.SERVICE_NAME }}/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            COMMIT=${{ github.sha }}
            COMMIT_MESSAGE=${{ steps.commit-msg.outputs.message }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: ${{ matrix.platform }}
          builder: ${{ steps.buildx.outputs.name }}

      - name: Build and push Audit Service Docker image (${{ matrix.platform }}) - attempt 3
        if: steps.build-attempt-2.outcome == 'failure'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/${{ env.SERVICE_NAME }}/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            COMMIT=${{ github.sha }}
            COMMIT_MESSAGE=${{ steps.commit-msg.outputs.message }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: ${{ matrix.platform }}
          builder: ${{ steps.buildx.outputs.name }}

