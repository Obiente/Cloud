name: Build and Push Support Service

on:
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - "apps/support-service/**"
      - "apps/shared/pkg/auth/**"
      - "apps/shared/pkg/database/**"
      - "apps/shared/pkg/health/**"
      - "apps/shared/pkg/logger/**"
      - "apps/shared/pkg/middleware/**"
      - "apps/shared/pkg/services/organizations/**"
      - "apps/shared/proto/obiente/cloud/auth/**"
      - "apps/shared/proto/obiente/cloud/support/**"
      - "packages/proto/proto/obiente/cloud/auth/**"
      - "packages/proto/proto/obiente/cloud/support/**"
      - "apps/shared/go.mod"
      - "apps/shared/go.sum"
      - ".github/workflows/build-support-service.yml"
      - ".github/workflows/reusable-container-build.yml"
  pull_request:
    branches:
      - main
    paths:
      - "apps/support-service/**"
      - "apps/shared/pkg/auth/**"
      - "apps/shared/pkg/database/**"
      - "apps/shared/pkg/health/**"
      - "apps/shared/pkg/logger/**"
      - "apps/shared/pkg/middleware/**"
      - "apps/shared/pkg/services/organizations/**"
      - "apps/shared/proto/obiente/cloud/auth/**"
      - "apps/shared/proto/obiente/cloud/support/**"
      - "packages/proto/proto/obiente/cloud/auth/**"
      - "packages/proto/proto/obiente/cloud/support/**"
      - "apps/shared/go.mod"
      - "apps/shared/go.sum"
      - ".github/workflows/build-support-service.yml"
      - ".github/workflows/reusable-container-build.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Support Service
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-container-build.yml
    with:
      service-name: support-service
      display-name: Support Service
      publish: false

  publish:
    name: Publish Support Service
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/reusable-container-build.yml
    with:
      service-name: support-service
      display-name: Support Service
      publish: true
    secrets: inherit
