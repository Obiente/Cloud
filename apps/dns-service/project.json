{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "dns-service",
  "root": "apps/dns-service",
  "projectType": "application",
  "implicitDependencies": ["shared"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "/usr/local/go/bin/go test -count=1 ./..."
      }
    }
  }
}
