# Dependencies
node_modules
**/node_modules

# Build outputs (excluded to force fresh builds, but cache mounts handle persistence)
# Note: We exclude these from COPY but use cache mounts in Dockerfile
.nuxt
.nitro
.output
dist
**/.nuxt
**/.nitro
**/.output
**/dist

# Development files
.git
.gitignore
*.md
.env
.env.*
!.env.example

# IDE
.vscode
.idea
*.swp
*.swo
*~

# Testing
coverage
.nyc_output

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Nx
.nx/cache
.nx/workspace-data

# TypeScript
*.tsbuildinfo
tsconfig.tsbuildinfo

# Note: We DO NOT exclude packages/proto/src here because dashboard needs it
# The root .dockerignore excludes it, but we'll use --ignorefile to override
